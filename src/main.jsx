import React from 'react'
import ReactDOM from 'react-dom/client'
import SegundaTela from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render( // Renderiza o componente SegundaTela na tela
  <React.StrictMode>
    <SegundaTela />
  </React.StrictMode>,
)
